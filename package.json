{
  "name": "opencode-orchestrator",
  "version": "1.0.0",
  "description": "AI assistant orchestration platform with OpenCode integration",
  "main": "index.js",
  "scripts": {
    "test": "nx run-many --target=test --projects=@workspace/webapp,@workspace/backend,@workspace/worker",
    "test:watch": "nx run-many --target=test:watch --projects=@workspace/webapp,@workspace/backend,@workspace/worker",
    "dev": "nx run @workspace/webapp:dev",
    "setup": "node scripts/setup.js",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format .",
    "format:fix": "biome format --write .",
    "typecheck": "nx run-many --target=typecheck --projects=@workspace/webapp,@workspace/backend,@workspace/worker",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,json,md}": [
      "biome format --write --no-errors-on-unmatched",
      "biome check --write --no-errors-on-unmatched"
    ]
  },
  "keywords": [
    "opencode",
    "orchestrator",
    "ai-assistant",
    "convex",
    "nextjs"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@nx/workspace": "21.4.1",
    "@types/node": "^20.17.24",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "nx": "21.4.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@radix-ui/react-tooltip": "^1.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "bun",
      "esbuild",
      "nx",
      "sharp",
      "unrs-resolver"
    ],
    "overrides": {
      "@radix-ui/react-dismissable-layer": "1.1.10"
    }
  }
}
